#name: Generate and Deploy API Docs
#
#on:
#  push:
#    branches: [ main ]
#  workflow_dispatch:
#
#jobs:
#  generate-and-deploy-docs:
#    runs-on: ubuntu-latest
#
#    steps:
#      - name: Checkout repository
#        uses: actions/checkout@v3
#        with:
#          fetch-depth: 0  # 브랜치 생성 및 푸시 위해 전체 히스토리 필요
#
#      - name: Set up JDK 23
#        uses: actions/setup-java@v3
#        with:
#          distribution: temurin
#          java-version: 23
#
#      - name: Grant execute permission for Gradle wrapper
#        run: chmod +x ./gradlew
#
#      - name: Run tests to generate snippets
#        run: ./gradlew :adapter-inbound:test
#
#      - name: Generate HTML documentation
#        run: ./gradlew :docs:generateDocs
#
#      - name: Configure Git
#        run: |
#          git config --global user.name "github-actions[bot]"
#          git config --global user.email "github-actions[bot]@users.noreply.github.com"
#
#      - name: Commit and push generated docs
#        run: |
#          git checkout -B docs
#          cp docs/build/docs/asciidocGenerateDocs/index.html .
#          git add .
#          git commit -m "Update API documentation [skip ci]" || echo "No changes to commit"
#          git remote set-url origin https://x-access-token:${GITHUB_TOKEN}@github.com/eunhwa99/SpecGenerator.git
#          git push -u origin docs --force
#        env:
#          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
